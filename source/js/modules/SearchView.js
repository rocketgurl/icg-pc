// Generated by CoffeeScript 1.3.3
(function() {

  define(['BaseView', 'mustache', 'Messenger', 'text!templates/tpl_search_container.html'], function(BaseView, Mustache, Messenger, tpl_search_container) {
    var SearchView;
    return SearchView = BaseView.extend({
      events: {
        "submit .filters form": "search"
      },
      initialize: function(options) {
        this.el = options.view.el;
        return this.$el = options.view.$el;
      },
      render: function() {
        var html;
        html = Mustache.render($('#tpl-flash-message').html(), {
          cid: this.cid
        });
        html += Mustache.render(tpl_search_container, {
          cid: this.cid
        });
        this.$el.html(html);
        return this.messenger = new Messenger(this.options.view, this.cid);
      },
      search: function(e) {
        var search_val;
        e.preventDefault();
        search_val = this.$el.find('input[type=search]').val();
        return this.Amplify.publish(this.cid, 'notice', search_val);
      }
    });
  });

}).call(this);
